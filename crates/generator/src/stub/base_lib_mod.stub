pub mod lexers;
use crate::lexers::*;

pub fn render_html(input: Vec<char>, lang: &str) -> String {
    return match lang {
        "name" => name::render::render_html(input),
        _ => String::new(),
    };
}

