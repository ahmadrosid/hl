constant: "nil,full"
entity_prefix:
  colon: ":"
  left_parent: (
condition:
  ACCEPT_STRING_DOUBLE_QUOTE: true
  ACCEPT_ENTITY_TAG_SUFFIX: self.ch.is_numeric()
  BREAK_ENTITY_TAG_SUFFIX: "!self.ch.is_numeric() && !is_letter(self.ch)"
  ACCEPT_CHAR_IDENTIFIER: "./-!?"
  PREFIX_ONE_LINE_COMMENT: ;;
keyword: "binding,case,catch,cond,do,ns,def,defonce,defmulti,defmethod,defn,if,fn,require,when,try,throw,for,let,defn-,in-ns,if-let,s/defn,if-not,when-not"