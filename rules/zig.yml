keyword:
  const: const
  pub: pub
  fn: fn
  try: try
  struct: struct
  return: return
  and: and
  or: or
  var: var
  error: error
  break: break
  continue: continue
  unreachable: unreachable
  while: while
  test: test
  enum: enum
  union: union
  async: async
  await: await
  suspend: suspend
  resume: resume
  nosuspend: nosuspend
  comptime: comptime
  extern: extern
  export: export
  threadlocal: threadlocal
  if: if
  orelse: orelse
  catch: catch
  for: for
  anytype: anytype
  volatile: volatile
  align: align
  allowzero: allowzero
  packed: packed
  switch: switch
  inline: inline
  else: else
  opaque: opaque
  callconv: callconv
  defer: defer
  errdefer: errdefer
  usingnamespace: usingnamespace
  asm: asm
  anyframe: anyframe
  noalias: noalias
  linksection: linksection